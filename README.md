# Flexbox space-between unexpected behavior

This repository demonstrates a common issue encountered when using `justify-content: space-between` in CSS Flexbox.  While `space-between` effectively distributes two items, adding more than two leads to overlapping elements instead of even spacing. The solution showcases how to achieve uniform distribution of multiple items.